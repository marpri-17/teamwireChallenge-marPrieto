import React from 'react';
import { Typography } from '@material-ui/core';

const Header = () => {
    return (<header className="page_header">
        <Typography variant="h3" className="page__title">My Personal Wish List</Typography>
    </header >)
}

export default Header;